# סַהַר — סטטוס פרויקט

_עדכון אחרון: 2026-08-31 (אחרי הוספת מדיניות)_

מותג תכשיטי כסף 925 (במקור "אֶמֶשׁ" — אתר בגדי פשתן, שונה לחלוטין לתכשיטים ולשם "סַהַר").
"סַהַר" = ירח, וגם "סַהֲרוֹנִים" הוא השם המקראי לתליוני כסף בצורת סהר (שופטים ח, כא; ישעיהו ג, יח).

יש שני קודי-בסיס נפרדים לאותו מותג:

## 1. אתר סטטי (GitHub Pages) — לצפייה/דמו בלבד

- **קוד**: `index.html` + `img/` בשורש הריפו
- **חי ב**: https://ofir6188-lang.github.io/emesh/
- **ריפו**: https://github.com/ofir6188-lang/emesh (ציבורי!)
- אין לו backend — לא יכול לעולם לעבד תשלומים אמיתיים. משמש כתצוגה/דמו בלבד.
- כל התמונות: תמונות סטוק מ-Unsplash (לא תמונות מוצר אמיתיות).

## 2. תבנית Shopify — "אתר המכירה האמיתי"

- **קוד**: `shopify-theme/` בריפו (Online Store 2.0 theme, מלא: layout/sections/snippets/templates/locales)
- **חנות פיתוח**: `choen-bjjqqny5.myshopify.com`
- **סיסמת חנות** (storefront password, נדרשת כי החנות עדיין לא הושקה): `elyean`
- **תבנית חיה (published)**: שם "סַהַר", theme id `149532934253`
- **חשבון Shopify**: `ofir6188@gmail.com`
- **אפליקציה מותאמת אישית ל-API**: "cohen claude" (app id `414448549889`), עם Admin API access token.
  - ⚠️ הטוקן עצמו **לא** נשמר כאן או בגיט (הריפו ציבורי!) — הוא ב-admin תחת Settings → Apps and sales channels → Develop apps → cohen claude → API credentials. אם אבד, אפשר לחשוף/ליצור חדש משם.
  - Scopes שהופעלו: `write_products`, `read_products`, `write_files`, `write_publications`, `read_publications`, `write_themes`, `read_themes`, `write_legal_policies`.
  - שימוש: עדכוני קוד לתבנית ויצירת מוצרים/אוספים נעשים ישירות דרך ה-Admin API (REST assets endpoint + GraphQL), **לא** דרך ה-Shopify CLI — ה-CLI צריך login נפרד (OAuth device-code) שפג תוקף כמה פעמים בין סשנים ודרש אישור ידני מהמשתמש בדפדפן בכל פעם.

### מוצרים אמיתיים בחנות (6)
כל התמונות דרך `img_base = https://ofir6188-lang.github.io/emesh/img/`, מ-CDN של שופיפיי עכשיו.

| שם | סוג | מחיר | קובץ תמונה מקורי |
|---|---|---|---|
| שרשרת יפו | כסף 925 | 420 | necklace1.jpg |
| שרשרת נמל | כסף 925, זירקון | 540 | necklace2.jpg |
| צמיד עמק | כסף 925 | 380 | bracelet1.jpg |
| צמיד שרב | כסף 925 | 460 | bracelet2.jpg |
| תליון מעיין | כסף 925 | 310 | pendant1.jpg |
| תליון תמר | כסף 925 | 480 | pendant2.jpg |

⚠️ **המחיר מוצג ב-$ (USD) לא ב-₪** — צריך שינוי ידני ב-Settings → General → Store currency.
17 מוצרי דמו (Gift Card + סנובורדים) שהגיעו עם החנות **נמחקו**.

### אוספים אמיתיים (3)
`necklaces`, `bracelets`, `pendants` — כרטיסיות הקטגוריה בעמוד הבית מקושרות אליהם.

## מה שונה מהעיצוב המקורי (כרונולוגי)
1. פשתן → תכשיטים, אֶמֶשׁ → סַהַר (תוכן + תמונות מלא)
2. עגלה/checkout אמיתיים דרך Shopify (לא היה אפשרי באתר הסטטי)
3. עיצוב "בוטיק אמנותי כהה" → פלטה בהירה עם אדום-פודרה, כפתורים מלאים עגולים, רצועת הכרזה, תגי אמון, "הוספה מהירה"
4. עוד מעבר: טיפוגרפיה sans-serif מודגשת (במקום סריף דק), רשת מוצרים צפופה יותר, כרטיסי מוצר לבנים במקום תמונה אווירתית מלאה, הסרת תוויות "FIG.", תג "חדש" אמיתי (מבוסס תאריך יצירה, לא מזויף)
5. תוקן באג אמיתי: `default`+`presets` לא יכולים לדור יחד בסכימת סקשן; blocks חייבים הגדרה מפורשת ב-JSON template (לא מספיק schema-level default)
6. אוספים אמיתיים לקטגוריות + structured data (JSON-LD) לעמודי מוצר
7. 4 מדיניות אמיתיות (משלוחים/החזרות 14 יום לפי חוק הגנת הצרכן/פרטיות/תנאי שימוש) דרך `shopPolicyUpdate` API — פוטר מקושר אליהן דרך `shop.policies` (במקום תפריט navigation ריק). עמודת "חנות" בפוטר גם תוקנה מאותה סיבה (קישורים ישירים במקום `section.settings.shop_menu` שהיה ריק).

## מה נשאר לעשות
- [x] ~~דפי מדיניות (משלוחים/החזרות/פרטיות)~~ — הושלם 2026-08-31
- [ ] תפריט ניווט עליון (header) מציג "Home/Catalog/Contact" באנגלית (ברירת מחדל של שופיפיי) במקום עברית — לא שבור, רק לא ממותג. דורש scope נוסף (navigation/menu) אם רוצים לתקן דרך ה-API.
- [ ] תמונות מוצר אמיתיות — המשתמש שלח 3 תמונות (שרשראות מסובכות על שטיח, צמיד עור על שולחן, צמיד עם אבנים ביד) — כולן ברקע עמוס/לא מקצועי. הומלץ לצלם מחדש על דף לבן ליד חלון. **טרם עובד/הועלה.**
- [ ] מטבע ל-₪, שם חנות ל"סַהַר" — Settings → General (ידני, לא API)
- [ ] סליקה אמיתית + שדרוג לתוכנית בתשלום — דורש פרטי עסק/בנק (ידני לגמרי)
- [ ] דומיין מותאם אישית — דורש רכישה (ידני)
- [ ] תפריט ניווט (header) לא מקושר לקטגוריות האמיתיות עדיין (רק כרטיסיות עמוד הבית)
- [ ] קידום/שיווק — רק אחרי שהחנות "חיה" באמת (סליקה+דומיין)

## פתרון בעיות נפוצות
- **"CLI login expired"**: אל תנסה `shopify auth login` דרך טרמינל לא-אינטראקטיבי בלי המשתמש בצד. עדיף: עדכוני קוד → REST Admin API assets endpoint ישירות עם הטוקן (ראה היסטוריית git למשל הסקריפט שנמחק `.deploy_assets.mjs`).
- **"404 בכל דף"**: בדוק קודם שגיאות schema אמיתיות (`shopify theme check` או ניסיון push וקריאת השגיאה), לפני שמניחים שזו הגדרת חנות (B2B/סיסמה).
- **תוכן ריק בסקשן עם blocks**: schema-level `"default"` לא חל ב-runtime על templates JSON — צריך `"blocks"` + `"block_order"` מפורשים בקובץ ה-JSON.
- Node מותקן ב-`C:\Program Files\nodejs` — לא ב-PATH כברירת מחדל בכלי ה-Bash; תמיד `export PATH="/c/Program Files/nodejs:/c/Users/97254/AppData/Roaming/npm:$PATH"` בתחילת פקודה.
